import * as React from 'react';
import { Typography } from '@mui/material';

export default function About() {
  return (
    <div>
      <Typography
        variant='h6'
        style={{ fontSize: '36px', color: '#9223FF' }}
      >
        About Me
      </Typography>
    </div>
  );
}
